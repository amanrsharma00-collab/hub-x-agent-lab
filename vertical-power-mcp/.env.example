# Local secrets — names only. Copy to .env (gitignored).

# IEA
IEA_API_KEY=
IEA_API_BASE_URL=

# Dataverse / Power Platform (Wave 1 — prefer device code; fill after validate)
DATAVERSE_URL=
# Optional later:
# AZURE_CLIENT_ID=
# AZURE_TENANT_ID=

# Power BI (often same account; fill if needed)
# POWERBI_WORKSPACE_ID=
